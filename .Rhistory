knitr::opts_chunk$set(echo = TRUE)
options(scipen=999)
pacman::p_load(shiny,
shinydashboard,
RSQLite,
tidyr,
dplyr,
ggplot2,
scales,
pROC,
DT,
lubridate,
XML,
caret,
quantmod,
kableExtra,
survival,
survminer,
caret,
ggplot2,
dplyr,
tidyr,
logger,
statsDK,
rentrez,
parallel,
foreach,
earth,
progress,
data.table,
glue,
gganimate)
source("~/NNEAP/my_efs/logger.R")
# filepath_nordnet = list.files(path = "~/NNEAP/my_efs/nordnet-etf",pattern = "fonde")
# filepath_abis = list.files(path = "~/NNEAP/my_efs/nordnet-etf",pattern = "abis")
filepath_nordnet = list.files(pattern = "fonde")
filepath_abis = list.files(pattern = "abis")
